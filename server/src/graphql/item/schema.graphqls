extend type User {
  _empty: String
}

type Item {
  id: ID
  title: String
  created_by: User
  created_at: String
}

type Query {
  getItems: [Item]
}

type Mutation {
  addItem(title: String!): Item
}
